// the .nextElementSibling return the next element after the specfic elemtn
const first = document.querySelector(".first");
const nextElement = first.nextElementSibling;
console.log(nextElement);

// the .previousElementSibling return the previous element
const last = document.querySelector(".last");
const previousElement = last.previousElementSibling;
console.log(previousElement);
